# Captcha-Automation
Deep learning-based CAPTCHA solver using CNN-RNN hybrid architecture with CTC loss, achieving ~99% accuracy across 5 government website types (GST, MSME, MCA, etc.). Deployed as a Flask-RESTx API on Azure with dynamic routing for real-time CAPTCHA decoding.
